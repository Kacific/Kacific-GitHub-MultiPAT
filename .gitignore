# direnv: per-machine token wiring
.envrc
!.envrc.example
!.envrc.example.*
.direnv/

# macOS
.DS_Store

# Session worktrees (per the managed concurrency block in AGENTS.md)
.claude/worktrees/

# Claude Code per-machine settings (one machine's tool permissions, not repo policy)
.claude/settings.local.json
